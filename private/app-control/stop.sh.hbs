etcdctl --peers {{etcdCluster}} set /instances/{{project}}/{{appName}}/{{instance}}/meta_/state "stopping"

{{#each services}}

etcdctl --peers {{../etcdCluster}} rm /skydns/ictu/{{../project}}/{{../instance}}/{{service}}
/opt/bin/delete-route.sh {{service}}-{{../project}}-{{../instance}}
docker stop -t 5 publicnetwork-{{service}}-{{../project}}-{{../instance}}
docker rm publicnetwork-{{service}}-{{../project}}-{{../instance}}
docker stop -t 30 {{service}}-{{../project}}-{{../instance}}

{{/each}}

etcdctl --peers {{etcdCluster}} rm /instances/{{project}}/{{appName}}/{{instance}} --recursive
